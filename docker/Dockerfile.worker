FROM python:3.14-slim

WORKDIR /app

COPY pyproject.toml /app/
RUN pip install -e ".[dev]"

COPY src/ /app/src/
COPY config/ /app/config/

ENV PYTHONPATH=/app/src

CMD ["celery", "-A", "core.tasks", "worker", "--loglevel=info"]
